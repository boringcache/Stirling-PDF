name: BoringCache build-cache proof

on:
  push:
    branches:
      - pr/native-boringcache-proof
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: boringcache-native-proof
  cancel-in-progress: true

env:
  IMAGE: ghcr.io/boringcache/stirling-pdf-fork-proof
  BORINGCACHE_WORKSPACE: stirling-tools/stirling-pdf

jobs:
  build:
    name: ${{ matrix.arch }} native 4-core build
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            platform: linux/amd64
            runner: ubuntu-latest
            cli_platform: linux-amd64
          - arch: arm64
            platform: linux/arm64
            runner: ubuntu-24.04-arm
            cli_platform: linux-arm64
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 90
    env:
      BORINGCACHE_RESTORE_TOKEN: ${{ secrets.BORINGCACHE_RESTORE_TOKEN }}
      BORINGCACHE_SAVE_TOKEN: ${{ secrets.BORINGCACHE_SAVE_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Verify native architecture
        env:
          EXPECTED_ARCH: ${{ matrix.arch }}
        run: |
          set -euo pipefail
          actual="$(uname -m)"
          case "${EXPECTED_ARCH}:${actual}" in
            amd64:x86_64|arm64:aarch64|arm64:arm64) ;;
            *)
              echo "Expected ${EXPECTED_ARCH}, got ${actual}" >&2
              exit 1
              ;;
          esac

      - name: Log in to GHCR
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121 # v4.1.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Build and push
        uses: boringcache/one@bd5597d3b41b5b52641c65787dcd1711fb1f675b # v1.13.97
        with:
          setup: none
          mode: docker
          workspace: ${{ env.BORINGCACHE_WORKSPACE }}
          cli-platform: ${{ matrix.cli_platform }}
          cache-runtime: "false"
          cache-backend: boringcache
          cache-tag: stirling-native-${{ matrix.arch }}
          context: .
          dockerfile: docker/embedded/Dockerfile
          image: ${{ env.IMAGE }}
          tags: run-${{ github.run_id }}-${{ github.run_attempt }}-${{ matrix.arch }}
          build-args: VERSION_TAG=cache-proof
          platforms: ${{ matrix.platform }}
          push: "true"
          provenance: "true"
          sbom: "true"
          fail-on-cache-error: "true"
          verify: wait
          diagnostics: summary

  gradle:
    name: Gradle build cache
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      BORINGCACHE_RESTORE_TOKEN: ${{ secrets.BORINGCACHE_RESTORE_TOKEN }}
      BORINGCACHE_SAVE_TOKEN: ${{ secrets.BORINGCACHE_SAVE_TOKEN }}
      STIRLING_FLAVOR: core
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Set up JDK 25
        uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # v5.2.0
        with:
          java-version: "25"
          distribution: temurin

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@3f131e8634966bd73d06cc69884922b02e6faf92 # v6.2.0
        with:
          gradle-version: 9.6.1
          cache-disabled: true

      - name: Set up Task
        uses: go-task/setup-task@01a4adf9db2d14c1de7a560f09170b6e0df736aa # v2.1.0

      - name: Set up Gradle cache
        uses: boringcache/one@bd5597d3b41b5b52641c65787dcd1711fb1f675b # v1.13.97
        with:
          setup: none
          mode: gradle
          workspace: ${{ env.BORINGCACHE_WORKSPACE }}
          cache-tag: stirling-gradle
          gradle-home: ~/.gradle
          enable-build-cache: "true"
          fail-on-cache-error: "true"
          verify: wait
          diagnostics: summary

      - name: Build backend
        run: task backend:build:ci

  rust:
    name: Linux Tauri with Gradle and sccache
    runs-on: ubuntu-22.04
    timeout-minutes: 45
    env:
      BORINGCACHE_RESTORE_TOKEN: ${{ secrets.BORINGCACHE_RESTORE_TOKEN }}
      BORINGCACHE_SAVE_TOKEN: ${{ secrets.BORINGCACHE_SAVE_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Install Linux desktop dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libappindicator3-dev \
            libgtk-3-dev \
            libjavascriptcoregtk-4.0-dev \
            libjavascriptcoregtk-4.1-dev \
            patchelf \
            librsvg2-dev \
            libsoup2.4-dev \
            libsoup-3.0-dev \
            libwebkit2gtk-4.0-dev \
            libwebkit2gtk-4.1-dev

      - name: Set up Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Set up JDK 25
        uses: actions/setup-java@be666c2fcd27ec809703dec50e508c2fdc7f6654 # v5.2.0
        with:
          java-version: "25"
          distribution: temurin

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@3f131e8634966bd73d06cc69884922b02e6faf92 # v6.2.0
        with:
          gradle-version: 9.6.1
          cache-disabled: true

      - name: Set up Task
        uses: go-task/setup-task@01a4adf9db2d14c1de7a560f09170b6e0df736aa # v2.1.0

      - name: Set up Gradle cache
        uses: boringcache/one@bd5597d3b41b5b52641c65787dcd1711fb1f675b # v1.13.97
        with:
          setup: none
          mode: gradle
          workspace: ${{ env.BORINGCACHE_WORKSPACE }}
          cache-tag: stirling-gradle
          gradle-home: ~/.gradle
          enable-build-cache: "true"
          save-always: "true"
          fail-on-cache-error: "true"
          verify: wait
          diagnostics: summary

      - name: Set up Rust cache
        uses: boringcache/one@bd5597d3b41b5b52641c65787dcd1711fb1f675b # v1.13.97
        with:
          setup: none
          mode: rust-sccache
          workspace: ${{ env.BORINGCACHE_WORKSPACE }}
          working-directory: frontend/editor/src-tauri
          cache-tag: stirling-tauri
          rust-version: stable
          cache-cargo: "true"
          cache-target: "false"
          sccache: "true"
          sccache-mode: proxy
          save-always: "true"
          fail-on-cache-error: "true"
          verify: wait
          diagnostics: summary

      - name: Prepare desktop build
        env:
          DISABLE_ADDITIONAL_FEATURES: "true"
          JPDFIUM_PLATFORMS: linux-x64
        run: task desktop:prepare

      - name: Run Tauri/Cargo tests
        run: task desktop:test

  manifest:
    name: Merge and verify manifest
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Log in to GHCR
        uses: docker/login-action@4907a6ddec9925e35a0a9e82d7399ccc52663121 # v4.1.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Merge platform images
        run: |
          set -euo pipefail
          prefix="${IMAGE}:run-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          docker buildx imagetools create \
            --tag "${prefix}" \
            "${prefix}-amd64" \
            "${prefix}-arm64"

          inspect="$(docker buildx imagetools inspect "${prefix}")"
          printf '%s\n' "$inspect"
          grep -q 'linux/amd64' <<< "$inspect"
          grep -q 'linux/arm64' <<< "$inspect"
